# Removing elements

## remove() method

This simply removes the target from the DOM. Not supported by internet explorer.

## removeChild() method

This method applied on the parent element does the same but is supported by all browsers.

```JavaScript
someElement.parentNode.removeChild(someElement)
```

## replaceChild replaceWith() methods

Simply replaces the targeted element.
