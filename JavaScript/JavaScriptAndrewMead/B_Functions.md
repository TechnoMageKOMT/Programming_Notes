# Functions

Second JavaScript course. Only new notes will be recorded.

## Undefined returns

This will happen

- When parameters are specified, but no arguments are passed to the function
- No return value is specified for the function and the "value" of the called function is then assigned to a variable.

